/**
 * Created by tom on 2017-02-20.
 */


function Matrix3(dim, array) {
    Matrix4.call(this, dim, array.slice(0,3));
}
Matrix3.inheritsFrom(Matrix4);

Matrix3.prototype.defineProperties({

});

Matrix3.defineProperties({

});