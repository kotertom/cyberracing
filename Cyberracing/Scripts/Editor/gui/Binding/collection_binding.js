/**
 * Created by tom on 2017-02-22.
 */



function CollectionBinding(domElement, obj, prop, converter) {
    Binding.call(this, domElement, obj, prop, converter);

}
CollectionBinding.inheritsFrom(Binding);

CollectionBinding.prototype.defineProperties({


});